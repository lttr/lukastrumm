[build]
  command = "npm run production"
  publish = "dist/"

[build.environment]
  NODE_VERSION = "12"

[[headers]]
  for = "/fonts/*.woff2"
  [headers.values]
    cache-control = "public, max-age=86400"